{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([cs_disco.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'cs_disco.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
